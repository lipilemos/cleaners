export const environment = {
  production: true,
  apiBaseUrl: 'https://api.cleaners.app/api',
  appVersion: '1.0.0',
  defaultTestLocation: undefined as
    { latitude: number; longitude: number } | undefined,
};
